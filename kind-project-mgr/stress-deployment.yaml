apiVersion: apps/v1
kind: Deployment
metadata:
  name: stress-deployment-1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stress
  template:
    metadata:
      labels:
        app: stress
    spec:
      schedulerName: my-custom-scheduler
      containers:
        - name: stress
          image: jfusterm/stress
          command: [ "stress", "--cpu", "80" ]
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "258Mi"
              cpu: "1024m"
